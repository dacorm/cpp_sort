#include "args.h"

void printHelp() {
    std::cout << "List of available arguments:" << std::endl << "\t-c sort from smallest to largest of the input entered into the console, the result is printed to the console" << std::endl;
}